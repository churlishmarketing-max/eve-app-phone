# EVE Desktop — artboards (v1)

Faithful artboards produced from uploads/EVE_DESKTOP_handoff.md, now on the **fused law** Brandon chose (candidate M2 + amendments): phone teal terminal-noir tokens + cockpit signatures — Space Mono chrome, red = both hot moments (live mic + RED tier), visor brackets, gold session numbering, teal waveform ring on the rail, ±1.6° cant. Gold stays tier/streaks; green stays the GREEN dot; the amber EVE OS system elsewhere is unchanged.

Boards: A deck hero · B summon overlay · C RED confirm · D body pane · E tray states · F settings/wire · G presence states · H summon states. Desktop dosing applied per handoff §3.6 (28px targets, 10px/.18em mono, hover lifts, 70% bubbles). v2 items render as locked/dashed rows only (wake word, calendar CLOCKS); v3 not drawn.

Laws honored: red = RED tier only; green only the GREEN autonomy dot; teal for done; no canned EVE lines (mock copy is from the handoff, marked chrome); honest offline/empty states (GOALS empty by design); quiet hours annotated; no secrets anywhere.

Portraits: her looks are real PNGs (768×1376) not included here — every portrait is a drop slot (drag AUTHORITY.png in; it persists).

Fusion exploration preserved in ui_kits/eve-fusion/ (M1 amber deck vs M2 teal cockpit — M2 chosen).

Status: kept as the shipped-app spec. The forward design direction is the paper world at ui_kits/eve-paper/ (chosen from redesign candidates).
