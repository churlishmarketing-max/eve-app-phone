// The canonical offline line — owning stream: S2.
//
// handoff §8 law 3, verbatim: every pane ships with distinct online-empty vs
// offline copy, and this is the offline one. It is CHROME, not her voice — she
// never speaks a canned line (law 2), so this sentence describes the screen,
// not her.
export const SHELL_COPY =
  "Her brain is unreachable, so this screen is a shell. It fills in the moment she answers.";
